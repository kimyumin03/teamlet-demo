# Teamlet — 프로젝트 포트폴리오

> 풀스펙 한국형 HR SaaS를 직접 설계·개발한 프로젝트 기록입니다.
> 📌 *기간 · 역할 · 팀 구성 등 개인 정보는 아래 [개요](#1-프로젝트-개요)의 표를 본인 상황에 맞게 채워 주세요.*

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| **프로젝트명** | Teamlet — 한국형 HR SaaS |
| **소속** | 대모산개발단 |
| **기간** | _(예: 2026.05 ~ 2026.06)_ |
| **역할** | _(예: 풀스택 단독 개발 / 백엔드·인프라 담당 등)_ |
| **인원** | _(예: 1인 / N인)_ |
| **배포** | axhub 플랫폼 (Docker) · <https://teamlet-app.jocodingax.axhub.ai> |
| **저장소** | github.com/kimyumin03/teamlet-demo |

**한 줄 소개** — 기존 HR SaaS(Flex)를 대체하는 것을 목표로, 한국 **근로기준법**을 정확히 반영한 멀티테넌트 인사관리 플랫폼을 기획부터 설계·구현·배포·운영까지 직접 수행했습니다.

**문제의식** — 시중 HR 솔루션은 ① 연차 부여·촉진 같은 법정 로직을 단순화하거나, ② 인사 변경 이력을 덮어쓰기로 처리해 시점 추적이 불가능하거나, ③ 권한을 역할 단위로만 다뤄 부서·본인 범위 제어가 약한 경우가 많습니다. Teamlet은 이 세 가지를 정면으로 해결하는 것을 핵심 가치로 잡았습니다.

---

## 2. 기술 스택

| 레이어 | 기술 |
|---|---|
| Frontend / Framework | Next.js 15 (App Router · RSC · Server Actions), React 19, TypeScript(strict) |
| Styling | Tailwind v4, 자체 디자인 토큰(Cool Slate), Lucide |
| Backend / Data | Prisma + Kysely, PostgreSQL, Zod 스키마 검증 |
| Auth | Auth.js v5 (Credentials + Google OAuth + TOTP 2FA) |
| Async | BullMQ + Redis (연차 자동부여·소멸·촉진 cron) |
| Infra | Turborepo + pnpm 모노레포, Docker, axhub 배포, Neon(serverless PostgreSQL) |

---

## 3. 주요 구현

- **멀티테넌시 & 인증** — `User`(인증) ↔ `Company` 다대다 멤버십 모델. 회사별 신분을 `Employee`로 분리. 이메일 2-step 로그인 + TOTP 2FA + 플랫폼 관리자 2단계 비밀키 콘솔.
- **RBAC + Scope 권한** — 역할 권한에 **범위(전체/부서/직속/본인)**와 **동적 역할(조직장)**을 결합. 모든 mutation 진입점에 `assertPermission` 가드 강제.
- **시점 기반 인사 이력** — 직책·부서 변경을 `UPDATE`가 아닌 `Appointment` 불변 row로 누적. 임의 시점의 조직 상태를 재구성 가능.
- **휴가 도메인 (근로기준법)** — 연차 자동부여 엔진(§60), 사용 촉진 엔진(§61), 법정휴가 자동 등록, 신청→전자결재 연동, 관리자 4탭 + 엑셀 입출력, 공휴일 자동 동기화.
- **통합 전자결재** — 휴가·공지·정보변경·할일을 단일 `FormDocument` + `ApprovalPolicy`로 통합. 순차 결재선 자동 배정 + 참조자 알림.
- **그 외** — 채용(후보자 칸반), 문서·증명서 발급, 인앱+SSE 알림, ⌘K 명령 팔레트, 홈 대시보드, 온보딩 체험 데모 모드.

> 도메인별 상세 기능은 [`README.md`](./README.md), 세션별 개발 기록은 [`PROGRESS.md`](./PROGRESS.md) 참고.

---

## 4. 기술적 도전 & 해결

### ① 근로기준법을 코드로 정확히 옮기기
- **문제**: 연차는 1년 미만 입사자에게 매월 1일씩 누적(§60②), 1년 이상은 법정 테이블(15일·3년부터 2년마다 +1·상한 25)로 부여되고, 미사용 시 사용자에게 사용을 **촉진**(§61)해야 법적 효력이 생깁니다. 회계연도/입사일 기준에 따라 소멸일도 달라집니다.
- **해결**: 부여·소멸·촉진을 각각 **멱등(idempotent) 엔진**으로 분리하고, 소멸일 계산을 단일 함수로 공유. 촉진은 `LeavePromotion` + 전자문서(전자결재) 연동으로 법적 효력을 갖도록 설계. 잘못 부여된 데이터를 안전하게 재계산할 수 있게 마커(`expiryProcessedAt`)로 1회 처리 보장.
- **결과**: Flex 캡처 수백 장을 전수 분석해 도출한 명세를 충족하는 법정 정확도 확보.

### ② "이력은 덮어쓰지 않는다" — 시점 기반 데이터 모델
- **문제**: 인사 발령(부서·직책 변경)을 컬럼 `UPDATE`로 처리하면 과거 시점 조직 상태를 잃습니다.
- **해결**: 모든 변경을 불변 `Appointment` row로 기록하고, `Employee`의 현재 부서·직책은 최신 row를 반영하는 비정규화 캐시로 취급. `getEmployeePositionAt(id, date)` 같은 시점 쿼리 제공.
- **결과**: 임의 과거 시점의 조직도/소속을 재구성 가능. (이를 강제하기 위해 "UPDATE 금지"를 프로젝트 Anti-Pattern으로 문서화)

### ③ 멀티테넌트 데이터 격리
- **문제**: 단일 DB에 다수 회사 데이터가 공존 → 회사 간 데이터 유출(IDOR) 위험.
- **해결**: 모든 쿼리에 `companyId` 필터를 강제하고, 권한 범위(Scope)와 결합. 자체 코드 감사로 12개 함수의 테넌트 격리 누락을 발견·수정.

### ④ axhub 배포 — 빌더 메모리 한계 정면 돌파
- **문제**: 배포 플랫폼(axhub)의 빌더가 메모리(OOM) 제약으로 풀스택 Next.js `next build`를 통과하지 못함. 사전 빌드 이미지 배포 옵션도, 빌드 메모리 상향 옵션도 없음.
- **해결**: **빌드 외부화** 전략 — 로컬 Docker에서 standalone 산출물을 만들어 `prebuilt.tar.gz`로 묶고, 별도 `axhub-deploy` 브랜치에 *압축 해제 후 실행만 하는* COPY-only Dockerfile과 함께 커밋. axhub 빌더는 빌드 없이 패키징만 수행 → OOM 원천 차단. 추가로 런타임이 외부 5432 포트를 차단하는 문제는 Prisma를 **Neon serverless 드라이버(443/WebSocket)**로 전환해 해결.
- **결과**: 풀스펙 모노레포 앱을 제약 많은 PaaS에 실제 배포·운영.

### ⑤ 운영 중 버그를 로그로 추적해 해결 (실제 디버깅 사례)
- **문제**: 배포 후 데모 계정의 로그아웃 시 데이터 초기화가 동작하지 않음. UI상 원인 불명(에러가 `try/catch`로 삼켜져 표면화되지 않음).
- **해결**: 런타임 로그에서 `update or delete on table "employees" violates RESTRICT ... announcements_authorId_fkey`를 발견. 체험 중 생성된 공지가 작성자(직원)를 RESTRICT 외래키로 잡아 삭제 트랜잭션 전체가 롤백된 것. Prisma 스키마를 전수 점검해 **Employee를 참조하는 모든 RESTRICT FK + 회사범위 전 테이블**을 FK-안전 순서로 선삭제하도록 정리 함수를 재설계.
- **결과**: 재배포 후 로그 검증으로 오류 0건 확인, 정상 초기화 복구. (원인→로그→스키마 분석→수정→재배포→검증의 전체 사이클 수행)

---

## 5. 성과 · 배운 점

- **기획→설계→구현→배포→운영**의 전 과정을 단독으로 수행하며, 도메인 지식(노동법)을 코드로 옮기는 과정을 경험.
- **불변 이력 모델 / 멱등 엔진 / 권한 범위 설계** 등 데이터 정합성을 우선하는 설계 습관을 체득.
- 제약이 큰 실제 배포 환경에서 **인프라 우회 전략**(빌드 외부화, serverless 드라이버 전환)을 직접 고안·적용.
- 표면에 드러나지 않는 운영 버그를 **로그·스키마 기반으로 추적**해 해결하는 디버깅 역량.
- 모든 변경을 PROGRESS/감사 문서로 남기고, 반복 실수를 막는 git 훅·컨벤션을 도입해 **유지보수성**을 의식한 개발.

---

## 6. 참고 링크

- 라이브 데모 · 데모 계정 — [`README.md`](./README.md#-데모-계정)
- 아키텍처 · 코딩 컨벤션 · Anti-Pattern — [`CLAUDE.md`](./CLAUDE.md)
- 세션별 개발 기록 · 배포 트러블슈팅 — [`PROGRESS.md`](./PROGRESS.md)
- 도메인 명세 · Flex 갭 분석 · 코드 감사 — [`/docs`](./docs)

> _이 문서는 프로젝트 사실(코드베이스·커밋·문서)에 근거해 작성되었습니다. 발표·이력서용으로 옮길 때는 본인의 역할과 기여 범위를 명확히 표기해 주세요._
