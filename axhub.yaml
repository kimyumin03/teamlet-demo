version: axhub/v1
name: teamlet-hr
runtime:
  port: 3000
  health_path: /api/health
build:
  deploy_method: docker
  dockerfile: Dockerfile
env:
  required:
    - name: DATABASE_URL
      scope: runtime
