# Generated by axhub init on 2026-05-27T08:54:45Z
version: 1
app:
    name: teamlet
    slug: teamlet
    framework: nextjs
runtime:
    port: 3000
    health_check: /api/auth/session
build:
    install: pnpm install --frozen-lockfile
    build: pnpm --filter web build
    start: pnpm --filter web start
deploy:
    target: auto
    environments:
        - staging
        - prod
